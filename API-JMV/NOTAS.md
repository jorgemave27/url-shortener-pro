# NOTAS - API JMV

## python-dotenv

Se usa para cargar variables desde un archivo `.env` en desarrollo, evitando hardcodear secretos (API_KEY, DATABASE_URL, etc.).
En producción, estas variables normalmente se inyectan por el entorno del servidor.
